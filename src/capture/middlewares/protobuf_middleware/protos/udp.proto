syntax = "proto3";

package udp;

message UdpSchema {
  // Packet id
  string id = 1;

  // Packet source port
  uint32 source = 2;

  // Packet destination port
  uint32 dest = 3;

  // Packet udp length
  uint32 len = 4;

  // Packet udp checksum
  uint32 check_p = 5;
}
